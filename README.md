# PyMicro
This is a code I wrote for testing. It is a 'useless piece of code' which creates a microservice written in Python using the FastAPI framework
This service uses a remote DB2 database to retrieve the data. 

** This is a work in progress. The code needs cleaning / refactoring **
