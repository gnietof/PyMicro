# PyMicro
This is a code I wrote for testing. It is a 'useless piece of code' which creates microservices written in Python using the FastAPI framework. SQLAlchemy/Pydantic are also used for dealing with the data structures.

These services also use a remote DB2 database to retrieve the data. 

** This is a work in progress. The code needs cleaning / refactoring **
